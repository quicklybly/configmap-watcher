apiVersion: v1
kind: Service
metadata:
  name: test-application
  labels:
    app: test-application
spec:
  type: ClusterIP
  selector:
    app: test-application
  ports:
    - name: http
      port: 8080
      targetPort: http
